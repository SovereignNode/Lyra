routes:
  /signup/: members/signup
  /signin/: members/signin
  /account/: members/account

collections:
  /:
    permalink: /{slug}/
    template: index
  /informatie/:
    permalink: /informatie/{slug}/
    template: index
    filter: primary_tag:informatie
  /gerechten/:
    permalink: /gerechten/{slug}/
    template: index
    filter: primary_tag:gerechten

taxonomies:
  tag: /tag/{slug}/
  author: /author/{slug}/
